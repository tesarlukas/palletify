import { Box, Text, VStack } from 'native-base';
import { FC } from 'react';

const Palettes: FC = () => {
    return (
        <Box>
            <Text>This is Palettes.</Text>
        </Box>
    );
};

export default Palettes;
